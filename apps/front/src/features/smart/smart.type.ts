export type SmartStageCard = {
    key: string;
    label: string;
    value: string;
    className: string;
};

export type SmartScreenContent = {
    statusLabel: string;
    weekLabel: string;
    weekValue: string;
    kicker: string;
    title: string;
    description: string;
};
