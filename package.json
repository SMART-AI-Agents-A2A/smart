{
    "name": "smart",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
        "dev": "concurrently -n api,front,db-studio -c blue,green,magenta \"pnpm --filter api dev\" \"pnpm --filter front dev\" \"pnpm --filter api db-studio\"",
        "prepare": "vp config"
    },
    "devDependencies": {
        "concurrently": "catalog:",
        "vite-plus": "catalog:",
        "wrangler": "^4.98.0"
    },
    "engines": {
        "node": ">=22.12.0"
    },
    "packageManager": "pnpm@10.33.0"
}
